def area(a, h):
    ''' Принимает числа a, h, возвращает их произведение, делёное на 2. '''
    return a * h / 2

def perimeter(a, b, c):
    ''' Принимает числа a, b, c, возвращает их сумму. '''
    return a + b + c
